name: flash_newpipe_extractor
description: A new flutter plugin project.
version: 0.0.1
homepage: null

environment:
  sdk: ">=2.12.0 <3.0.0"
  flutter: ">=1.20.0"

dependencies:
  flutter:
    sdk: flutter
  http: ^0.13.3

dev_dependencies:
  flutter_test:
    sdk: flutter

flutter:
  plugin:
    platforms:
      android:
        package: kannel.outtis.flashEx.flash_newpipe_extractor
        pluginClass: FlashNewPipeExtractorPlugin
